# Optional: if the API is not on http://localhost:5000
# REACT_APP_API_URL=http://localhost:5000

# Card checkout: prefer backend-only setup (see backend/.env.example — STRIPE_PUBLISHABLE_KEY there).
# Optional legacy fallback (must restart `npm start` after changing):
# REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_...
